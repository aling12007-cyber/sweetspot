name: Patch runner
on:
  push:
    branches: [chatgpt/contact-case-study-layout]
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo ready
