name: One-time reference major headings

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/reference-major-headings.yml'

permissions:
  contents: write

jobs:
  update-headings:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply reference major heading format
        run: python .github/scripts/patch_reference_headings.py
      - name: Commit website update and remove helpers
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/reference-major-headings.yml
          git rm .github/scripts/patch_reference_headings.py
          git add index.html
          git commit -m "Apply Insights-style heading format sitewide"
          git push
