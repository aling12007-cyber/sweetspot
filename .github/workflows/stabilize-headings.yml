name: Stabilize heading system
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/stabilize-headings.yml'
permissions:
  contents: write
jobs:
  stabilize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Stabilize heading controller and verify runtime
        run: python .github/scripts/stabilize_headings.py
      - name: Commit stabilized heading system
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html .github/workflows/validate-heading-integrity.yml
          git rm -f --ignore-unmatch .github/scripts/stabilize_headings.py .github/workflows/stabilize-headings.yml
          git commit -m 'Stabilize heading source of truth and add regression guard'
          git push
